# Full TimeTracker stack (dev + single-VM production).
#   postgres  -> source of truth for synced activity (Rule 4)         :5432
#   server    -> Axum API server, built from ./Dockerfile              :9000
#   admin-web -> Next.js admin dashboard, built from ./Dockerfile.admin :3001 (internal)
#   nginx     -> reverse proxy / web entrypoint                        :80
#
# Object storage (Rule 5) is Google Cloud Storage. The server only mints V4
# signed URLs; screenshot bytes go straight from clients to the GCS bucket and
# are never proxied through this stack. Provide a service-account key via
# GCS_SA_KEY_JSON (inline JSON) in .env — see .env.example.
#
# The desktop app is a native Tauri app and is NOT containerized; it talks to the
# API directly on :9000 and uploads screenshots straight to the GCS bucket.
#
# Web entrypoint (browser): http://<VM_HOST>/  -> dashboard, http://<VM_HOST>/api -> API.
#
# Usage:
#   docker compose up -d --build   # build + start the whole stack
#   docker compose down            # keep volumes
#   docker compose down -v         # wipe volumes

name: timetracker

services:
  postgres:
    image: postgres:16-alpine
    container_name: timetracker-postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: timetracker
      POSTGRES_PASSWORD: timetracker
      POSTGRES_DB: timetracker
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U timetracker -d timetracker"]
      interval: 5s
      timeout: 5s
      retries: 10

  # API server — built from ./Dockerfile, exposed on :9000. Talks to postgres
  # over the compose network and to Google Cloud Storage over the internet.
  # Secrets are read from the project .env via ${VAR} interpolation (safe dev
  # defaults where harmless).
  server:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: timetracker-server
    restart: unless-stopped
    depends_on:
      postgres:
        condition: service_healthy
    environment:
      SERVER_HOST: 0.0.0.0
      SERVER_PORT: "9000"
      # Internal compose-network hostnames (NOT localhost).
      DATABASE_URL: postgres://timetracker:timetracker@postgres:5432/timetracker
      DATABASE_MAX_CONNECTIONS: "10"
      JWT_ACCESS_SECRET: ${JWT_ACCESS_SECRET:-dev-access-secret-change-me}
      JWT_REFRESH_SECRET: ${JWT_REFRESH_SECRET:-dev-refresh-secret-change-me}
      JWT_ACCESS_TTL_SECONDS: "900"
      JWT_REFRESH_TTL_SECONDS: "2592000"
      # Google Cloud Storage (Rule 5): the server signs V4 URLs; clients upload
      # bytes directly to the bucket. GCS_SA_KEY_BASE64 is the service-account
      # key JSON, base64-encoded (single line), granting object access on the bucket.
      GCS_BUCKET: ${GCS_BUCKET:-ruh-time-tracker}
      GCS_LOCATION: ${GCS_LOCATION:-auto}
      GCS_SA_KEY_BASE64: ${GCS_SA_KEY_BASE64:-}
      PUBLIC_BASE_URL: ${PUBLIC_BASE_URL:-http://localhost:9000}
      LINEAR_API_KEY: ${LINEAR_API_KEY:-}
      GEMINI_API_KEY: ${GEMINI_API_KEY:-}
      RUST_LOG: info,server=debug,sqlx=warn
    ports:
      - "9000:9000"

  # Admin dashboard (Next.js). NEXT_PUBLIC_API_BASE_URL is inlined at BUILD time,
  # so it is passed as a build arg (not a runtime env). Default "/api" => the
  # browser makes same-origin calls that Nginx routes to the API container.
  admin-web:
    build:
      context: .
      dockerfile: Dockerfile.admin
      args:
        NEXT_PUBLIC_API_BASE_URL: ${NEXT_PUBLIC_API_BASE_URL:-/api}
    container_name: timetracker-admin-web
    restart: unless-stopped
    depends_on:
      - server
    expose:
      - "3001"   # internal only; reached through Nginx

  # Reverse proxy / single web entrypoint on :80 (origin behind Cloudflare).
  #   /     -> admin-web:3001
  #   /api/ -> server:9000  (prefix stripped)
  nginx:
    image: nginx:1.27-alpine
    container_name: timetracker-nginx
    restart: unless-stopped
    depends_on:
      - admin-web
      - server
    ports:
      - "80:80"
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro

volumes:
  postgres_data:
