# Binary assets — never apply line-ending conversion (a CRLF rewrite corrupts them).
*.pdf binary
*.png binary
*.jpg binary
*.ico binary
*.icns binary

# SQL migrations must stay LF everywhere: sqlx embeds them and validates
# checksums against the applied DB; a CRLF checkout breaks every migrate run.
*.sql text eol=lf
